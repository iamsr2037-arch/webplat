// Prisma config file - configuration is handled via environment variables
// For Prisma 7, the datasource connection is passed to PrismaClient in lib/db.ts

export default {
  // This file serves as a marker for Prisma to recognize this as a Prisma project
  // Actual database configuration is handled in lib/db.ts via the adapter pattern
};
